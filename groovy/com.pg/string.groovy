package com.pg
def a=" wallal laa laa wa sada"
a=a.concat(" hello")
println a
println a.substring(5)
println(a-"laa ")
println(a.toList())
println(a[1..5])
println(a[5..1])
b=$/commom indian lets $a/$  //alternate way to do thing we can add " " with no probs in this case also / enter string / is also there
println(b)
println(a.split(" "))
println a.length()
println a.indexOf('l')//prints index of first occurence
println a.substring(2)
println a.substring(2,4)
println a-"hello"
println a.replace("l","d")//will replace all occurences
println a.toLowerCase()
println a.toUpperCase()
println a.toList()
println a.equals(" wallal laa laa wa sada hello")
println a.equalsIgnoreCase(" wallal laa laa wa sada hello")